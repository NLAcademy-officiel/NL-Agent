# lib/auth

Logique d'authentification (sessions, hashing, providers).

**Statut Phase 1** : non implémenté. Les pages `/auth/login` et
`/auth/register` sont des interfaces statiques sans logique de connexion
réelle. À implémenter en Phase 2 (ex. Auth.js / NextAuth, ou solution
maison avec `AUTH_SECRET`).
